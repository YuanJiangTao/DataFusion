<Border x:Class="DataFusion.Views.MainContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DataFusion.Views"
             mc:Ignorable="d" 
              Background="{DynamicResource MainContentBackgroundBrush}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="{DynamicResource MainContentForegroundDrawingBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <DockPanel Grid.Row="0">
                <TextBlock DockPanel.Dock="Top"  Style="{StaticResource TextBlockDefault}" HorizontalAlignment="Left" Foreground="White" Text="{Binding ContentTitle,StringFormat='当前位置:{0}'}"></TextBlock>
            </DockPanel>
            <Frame  Margin="5"  Grid.Row="1" VerticalAlignment="Stretch" 
                    NavigationUIVisibility="Hidden" 
                    HorizontalAlignment="Stretch"  Content="{Binding MainContent}">
                
            </Frame>
        </Grid>
    </Border>

</Border>
