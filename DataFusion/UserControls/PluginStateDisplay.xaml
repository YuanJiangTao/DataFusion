<UserControl x:Class="DataFusion.UserControls.PluginStateDisplay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DataFusion.UserControls"
             mc:Ignorable="d" 
             DataContext="{Binding }"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Expander Header="Title" BorderThickness="1,1,1,0" Margin="5" >
            <StackPanel>
                <Button Width="200" Content="Fist"></Button>
                <Button  Width="200" Content="Second"></Button>
            </StackPanel>
        </Expander>

    </Grid>
</UserControl>
